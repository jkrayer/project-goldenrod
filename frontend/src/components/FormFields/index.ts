import { Field as Root } from "./Field";
import { Input } from "./Input";
import { TextArea } from "./TextArea";
import { Error, Label } from "./Labels";

export const Field = {
  Root,
  Input,
  TextArea,
  Label,
  Error,
};
